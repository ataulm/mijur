subprojects {
    repositories {
        mavenCentral();
        maven {
            url "https://oss.sonatype.org/content/repositories/snapshots"
        }
    }
}

task wrapper(type: Wrapper) {
    gradleVersion = '1.8'
}

